function returnProducts(productsDAO, startDate, endDate, aggregation, platform, product) {

    //  ---- NOT IMPLEMENTED ---- //

    // let ProductsList = undefined;

    // function waitForProducts(callback){
    //     productsDAO.getSummary(startDate, endDate, aggregation, platform, null, function (err, result) {
    //         callback(result);

    //     });
    // };


    // function returningProduct(retorno) {
    //     this.ProductsList = retorno;
    //     console.log(ProductsList);
        
    // };

    // waitForProducts(returningProduct);
}

module.exports = returnProducts;